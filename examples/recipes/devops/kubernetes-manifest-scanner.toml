# Kubernetes Manifest Scanner
name = "K8s Manifest Scanner"

[[step]]
type = "filter"
pattern = 'privileged:\s*true|runAsRoot:\s*true'
action = "keep_line"
description = "Find privilege escalation"

[[step]]
type = "substitute"
pattern = '^(.*)$'
replacement = '[SECURITY] $1'
