# Helm Values Scanner
name = "Helm Values Scanner"

[[step]]
type = "filter"
pattern = '(password|secret|key|token):'
action = "keep_line"
case_insensitive = true

[[step]]
type = "substitute"
pattern = '^(.*)$'
replacement = '[SENSITIVE] $1'
