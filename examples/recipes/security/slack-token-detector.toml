# Slack Token Detector
name = "Slack Token Detector"

[[step]]
type = "filter"
pattern = 'xox[baprs]-[0-9]+-[0-9A-Za-z-]+'
action = "keep_line"

[[step]]
type = "substitute"
pattern = '^(.*)$'
replacement = '[SLACK TOKEN] $1'
